#define _CRT_SECURE_NO_WARNINGS 1
#include <stdio.h>
#include <stdlib.h>
#include <string>

int main() 
{
	int a, b;
	scanf("%d %d", &a, &b);
	for (int i = 0; i < a + b; i++)
	{
		printf("wang!");
	}
	return 0;
}