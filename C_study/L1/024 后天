#define _CRT_SECURE_NO_WARNINGS 1
#include <stdio.h>
#include <stdlib.h>
#include <string>

int main()
{
	int m;
	scanf("%d", &m);
	if (m >= 1 && m <= 5)
	{
		printf("%d", m + 2);
	}
	else
	{
		printf("%d", m - 5);
	}
	return 0;
}    